{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend for the security services booking website",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "mongoose": "^9.9.1",
    "nodemailer": "^9.0.3"
  }
}
